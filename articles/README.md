# 文章目录说明

每一篇文章都是一个独立的 HTML 片段文件，只需要编辑对应文件里的正文内容即可。

- 港卡：`articles/hk-card/`
- 券商：`articles/broker/`
- 跨境汇款：`articles/remit/`

注意：这些文件不是完整 HTML 页面，不需要写 `<html>`、`<head>`、`<body>`，直接写正文即可，例如 `<h2>`、`<p>`、`<ul>`、`<figure>`。
